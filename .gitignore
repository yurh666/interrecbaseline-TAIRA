# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
.idea/

# Local env (optional; you may track conda env.yml instead)
.venv/
venv/
.env.local

# Incremental run checkpoints (large CSVs; do not commit)
results/checkpoints/

# Regenerable experiment logs (per-query); keep result-*.csv in logs dirs
**/data/*/logs/**/*.log

# Redundant upstream bundle (tracked code lives under TAIRA/)
TAIRA_master.zip
